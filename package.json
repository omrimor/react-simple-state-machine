{
  "name": "react-simple-state-machine",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bulma": "^0.7.2",
    "lodash": "^4.17.11",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start:mockapi": "json-server --watch db/db.json --port 4001 --delay 3000",
    "start:dev": "react-scripts start",
    "start": "npm-run-all --parallel start:dev start:mockapi",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "json-server": "^0.14.0",
    "npm-run-all": "^4.1.3"
  }
}
